{% extends 'base.html.twig' %}

{% block title %}Formulaire d'ajout de catégorie{% endblock %}

{% block body %}
    <h1 class="text-center">Formulaire d'ajout de catégorie : </h1>

    {# Crud form #}
    {{ form_start(crud_form) }}
        <div class="row">
            <div class="mx-auto col-6">
                {{ form_row(crud_form.name) }}
                <button class="btn">{{ button_label|default('Save') }}</button>
                {{ form_end(crud_form) }}
            </div>
        </div>

    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}

